---
description: saw search — find the command you want by describing what you are trying to do.
---

# `saw search`

```text
saw search <text...> [--json] [-q] [--no-stream]
```

Fuzzy lookup over the whole command tree — `saw search "open a pr"` suggests `saw fix`. `-q` prints
only the matching command names.
