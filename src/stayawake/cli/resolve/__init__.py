#!/usr/bin/env python3
"""The interactive keep/remove resolver `saw fix amend` injects on a real terminal — a cli plug-in
the core calls but never imports."""
from stayawake.cli.resolve.build import build_resolver

__all__ = ["build_resolver"]
